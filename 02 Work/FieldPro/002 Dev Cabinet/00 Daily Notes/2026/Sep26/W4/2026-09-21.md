---
tags: [daily]
date: 2026-09-21
---
## 🎯 Habits

```habits
```
# 📅 2026-09-21

> **Daily engineering log** — plan → execute → capture → review.

## 🧠 Remember

> Things I want to keep visible every day.

```dataview
TABLE WITHOUT ID
  replace(item.text, "#remember", "") AS "Remember"
FROM ""
FLATTEN file.lists AS item
WHERE contains(item.text, "#remember")
SORT file.mtime DESC
```

### 🔄 Carried Over 
```dataview
TASK
FROM "02 Work/FieldPro/002 Dev Cabinet/00 Daily Notes"
WHERE !completed
AND file.name != dateformat(date(today), "yyyy-MM-dd")
SORT priority ASC
SORT due ASC
```

## 📝 Yesterday
```dataviewjs
const yesterday = window.moment().subtract(1, "day").format("YYYY-MM-DD");
const folder = "02 Work/FieldPro/002 Dev Cabinet/00 Daily Notes";

const pages = dv.pages(`"${folder}"`);
const page = pages.find(p => p.file.name === yesterday);

if (page) {
    const items = page.file.lists
        .filter(item => item.section?.subpath === "Today");

    dv.list(items.map(item => item.text));
} else {
    dv.paragraph(`No daily note found for ${yesterday}.`);
}
```
```dataviewjs
const currentDate = dv.current().file.name;

const yesterday = window.moment(currentDate, "YYYY-MM-DD", true)
    .subtract(1, "day")
    .format("YYYY-MM-DD");

const page = dv.pages("")
    .find(p => p.file.name === yesterday);

if (page) {
    const items = page.file.lists
        .filter(item =>
            item.section?.subpath &&
            item.section.subpath.trim().endsWith("Today")
        );

    if (items.length > 0) {
        dv.list(items.map(item => item.text));
    } else {
        dv.paragraph(`Found ${yesterday}, but no items were found under Today.`);
    }
} else {
    dv.paragraph(`No daily note found for ${yesterday}.`);
}
```
---
- working on 4041 TVP refactoring.
- left join vs not exists **done
- what happends if we sends multiple same ids.
- change Ids to IDs **done
- tickets.cs domain. and check the modle lenth if its emplty line 446
- use as enumrable instead of to list.
- clean up the model namespace. and try to use the domainbase instead of the datamodlebase. **done
- create a jobids's own type instead of using just UDTT_IDS **done
- delete the new helper method. **done
- check domainbase vs datamodelbase. **done
- check the code analysis link provided by jordon [ [SQL Code Analysis - SQL Server | Microsoft Learn](https://learn.microsoft.com/en-us/sql/tools/sql-database-projects/concepts/sql-code-analysis/sql-code-analysis?view=azuresqldb-current&pivots=sq1-visual-studio) ]
## 🎯 Today

**Top priority:**  

- [x] I did params with jobids for offline. and methods with just pasing ids and the one with named params ids.
- [x] use domain base instead of modelbase. changed the new sql pramar to use Addparam
	Today
- [x] Test Ticket_InsertForGroup you changed jordon. on the database cleanup branch.
- [x] I need to check the DateList.
- [x] and any other cases that i am not aware of.
- [x] check the code analysis link provided by jordon [ [SQL Code Analysis - SQL Server | Microsoft Learn](https://learn.microsoft.com/en-us/sql/tools/sql-database-projects/concepts/sql-code-analysis/sql-code-analysis?view=azuresqldb-current&pivots=sq1-visual-studio) ] ✅ 2026-09-24

**Other priorities:**  


## 🚧 Blockers

> Anything stopping progress today.

- None
---
, refactoring the tvp. incident and investigation security 4137
, will continue on the tvp I have around 70 more to go. then i will move back to database cleanup branch.
## 📦 Work Items

> Link the work, bug, feature, PR, or investigation you are actively touching.

- 

---

## 💡 Lessons Learned

> Capture discoveries here first. Promote durable knowledge to `04 Lessons Learned`.

- 
---

## 📝 Notes

- 
---

## 📊 End-of-Day Summary

**Status:** 🟡 In Progress

### Completed

- 

### Still in Progress

- 

### Blockers

- None

### Tomorrow

- 

---

## 🔗 Related

**Sprint:**  
- 

**PR / Ticket:**  
- 

**Useful notes:**  
- 
