---
tags: [daily]
date: 2026-09-23
---
## 🎯 Habits

```habits
```
# 📅 2026-09-23

> **Daily engineering log** — plan → execute → capture → review.

## 🧠 Remember

> Things I want to keep visible every day.

```dataview
TABLE WITHOUT ID
  replace(item.text, "#remember", "") AS "Remember"
FROM ""
FLATTEN file.lists AS item
WHERE contains(item.text, "#remember")
SORT file.mtime DESC
```

### 🔄 Carried Over 
```dataview
TASK
FROM "02 Work/FieldPro/002 Dev Cabinet/00 Daily Notes"
WHERE !completed
AND file.name != dateformat(date(today), "yyyy-MM-dd")
SORT priority ASC
SORT due ASC
```

## 📝 Yesterday
```dataviewjs
const currentDate = dv.current().file.name;

const yesterday = window.moment(currentDate, "YYYY-MM-DD", true)
    .subtract(1, "day")
    .format("YYYY-MM-DD");

const page = dv.pages("")
    .find(p => p.file.name === yesterday);

if (page) {
    const items = page.file.lists
        .filter(item =>
            item.section?.subpath &&
            item.section.subpath.trim().endsWith("Today")
        );

    if (items.length > 0) {
        dv.list(items.map(item => item.text));
    } else {
        dv.paragraph(`Found ${yesterday}, but no items were found under Today.`);
    }
} else {
    dv.paragraph(`No daily note found for ${yesterday}.`);
}
```
---
Yesterday 
I finished the refactoring and left only a few edge cases where the logic is more complicated and requires careful changes, such as `fnSplitIntWithOrder` and `ToolPlacementItem`.

I also left any split functions that operate on variables that are not passed as parameters to the stored procedure.

This morning
I review this morning and i found 1 issue that i fixed. now I will be testing **Ticket_InsertForGroup** 

## 🎯 Today

**Top priority:**  
- [x] I had meeting with jodon to discuss the split with order, then I Fixed and test the costitem to make sure its working as expected. looked good. ✅ 2026-09-24
- [x] fixed some issues for the tvp changes i made. ✅ 2026-09-24
- [x] tried but was not able to test the **Ticket_InsertForGroup** ✅ 2026-09-24

**Other priorities:** 


## 🏆 Wins

- 

## 🚧 Blockers

> Anything stopping progress today.

- None
---

## 📦 Work Items

> Link the work, bug, feature, PR, or investigation you are actively touching.

- 

---

## 💡 Lessons Learned

> Capture discoveries here first. Promote durable knowledge to `04 Lessons Learned`.

- 
---

## 📝 Notes

- ✅ opencode verification — 2026-09-23: accessed and modified `2026-09-23.md` (latest daily note under `00 Daily Notes`). 
- IN **LoadOut_DeleteSelected** LINE 205. it checks if the LoadOutThirdPartyID in @LoadOutThirdPartyIDs but the comment above says product. not sure which one is the incorrect. the comment or the check AND [tToolPlacementItem].LoadOutThirdPartyID in (select value from string_split(@LoadOutThirdPartyIDs, ','));
---

## 📊 End-of-Day Summary

**Status:** 🟡 In Progress

### Impact

> One line: what your work unlocked or improved today — feeds report Highlights.

- 

### Completed

- 

### Still in Progress

- 

### Blockers

- None

### Tomorrow

- 

---

## 🔗 Related

**Sprint:**  
- 

**PR / Ticket:**  
- 

**Useful notes:**  
- 
